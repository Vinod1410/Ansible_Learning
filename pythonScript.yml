---
 - name: deploy a web application
   hosts: {{ HOSTS }}
   gather_facts: yes

   tasks:
     - name: ping the server. 
       ping:
       debug:
         msg: "Print the reachable server {{ HOSTS }}"
